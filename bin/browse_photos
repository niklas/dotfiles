#!/usr/bin/env bash

destdir="$(sed "s/\s.*#.*$//;/^\s*$/d" ~/.bmdirs | awk '{print $2}' | dmenu -l 20 -i -p "Browse Photos where?" | sed "s|~|$HOME|g")"
[ -z "$destdir" ] && exit
sxiv -ft "${destdir}"