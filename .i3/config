# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $left h
set $right l
set $down j
set $up k
set $term --no-startup-id $TERMINAL
set $hibernate sudo -A systemctl suspend

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6:edit"
set $ws7 "7:www"
set $ws8 "8"
set $ws9 "9:mail"
set $ws10 "10:auto"

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
#font
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10
font pango:Inconsolata 15

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec $term

# kill focused window
bindsym $mod+Shift+q kill

bindsym $mod+Shift+BackSpace	exec --no-startup-id prompt "Reboot computer?" "$reboot"

# start dmenu (a program launcher)
bindsym $mod+i exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# Show selection:
bindsym $mod+Insert		exec --no-startup-id showclip
bindsym $mod+Pause		exec --no-startup-id ddspawn dropdowncalc -fn xft:Inconsolata:pixelsize=24:antialias=true
bindsym $mod+plus  exec --no-startup-id send_selection_to

# Handy programs
bindsym $mod+y exec $term -e $FILE
bindsym $mod+F4 exec $term -e htop
bindsym $mod+F6 exec em ~/org/Me.org; workspace --no-auto-back-and-forth $ws6
bindsym $mod+F9 exec thunderbird; workspace --no-auto-back-and-forth $ws9

# change focus
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Control+$left move left
bindsym $mod+Control+$down move down
bindsym $mod+Control+$up move up
bindsym $mod+Control+$right move right

# alternatively, you can use the cursor keys:
bindsym $mod+Control+Left move left
bindsym $mod+Control+Down move down
bindsym $mod+Control+Up move up
bindsym $mod+Control+Right move right

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Control+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Control+1 move container to workspace $ws1
bindsym $mod+Control+2 move container to workspace $ws2
bindsym $mod+Control+3 move container to workspace $ws3
bindsym $mod+Control+4 move container to workspace $ws4
bindsym $mod+Control+5 move container to workspace $ws5
bindsym $mod+Control+6 move container to workspace $ws6
bindsym $mod+Control+7 move container to workspace $ws7
bindsym $mod+Control+8 move container to workspace $ws8
bindsym $mod+Control+9 move container to workspace $ws9
bindsym $mod+Control+0 move container to workspace $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym o resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# thinkpad keys
bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 5 && pkill -USR1 i3blocks
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 10 && pkill -USR1 i3blocks
bindsym XF86AudioMicMute exec pactl set-source-mute 1 toggle && pkill -USR1 i3blocks
#bindsym XF86WLAN exec --no-startup-id amixer -- sset Mic toggle
bindsym XF86AudioLowerVolume exec pactl set-sink-volume 0 -5% && pkill -USR1 i3blocks
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume 0 +5% && pkill -USR1 i3blocks
bindsym XF86AudioMute exec pactl set-sink-mute 0 toggle && pkill -USR1 i3blocks
bindsym $mod+Shift+L exec lock_screen

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3blocks
        tray_output primary
        position top
        font pango:Inconsolata 9
}
workspace_auto_back_and_forth yes

# monitor rules
workspace $ws2  output VGA1
workspace $ws4  output VGA1
workspace $ws6  output LVDS1
workspace $ws8  output VGA1
workspace $ws10 output LVDS!
#
workspace $ws1 output LVDS1
workspace $ws3 output LVDS1
workspace $ws5 output LVDS1
workspace $ws7 output LVDS1
workspace $ws9 output VGA1

# operate workspace switching with thinkpad's extra arrow keys
bindsym $mod+F18 workspace next
bindsym $mod+F17 workspace prev
bindsym $mod+Shift+F18 move container to workspace next; workspace next
bindsym $mod+Shift+F17 move container to workspace prev; workspace next
# Move the current workspace to the next output
# (effectively toggles when you only have two outputs)
bindsym $mod+x move workspace to output right

bindsym $mod+Shift+V exec i3-input -F 'rename workspace to %s' -P 'Workspace name: '

bindsym $mod+Shift+B border toggle
bindsym Print exec screenshot
# cannot apply modifiere to Print key :(
#bindsym $mod+Print exec screenshot window
bindsym $mod+p exec browse_photos

assign [class="google-chrome" window_role="browser"] $ws7
assign [class="Pidgin"] "8:chat"
assign [class="chromium-browser"] $ws7
assign [class="Chromium-browser"] $ws7
# TODO keep the test browser from popping up in our face.
#assign [class="webdriver"] $ws10
#no_focus [instance="chromium-browser"]
#no_focus [instance="Chromium-browser"]
#for_window [instance="chromium-browser"] floating enable

# gnu radio
assign     [class="Top_block.py"] "3:radio"
for_window [class="Top_block.py"] fullscreen

# dropdowns
for_window [instance="dropdown_*"] floating enable
for_window [instance="dropdown_*"] move scratchpad
for_window [instance="dropdown_*"] sticky enable
for_window [instance="dropdown_*"] scratchpad show
for_window [instance="dropdowncalc"] resize set 800 300
for_window [instance="dropdowncalc"] border pixel 2
for_window [instance="dropdown_*"] move position center

# gnuradio
assign     [class="Top_block.py"] "3:radio"
for_window [class="Top_block.py"] fullscreen

# Games etc
for_window [class="org-spoutcraft-launcher-Main"] floating enable

# pop-ups
for_window [window_role="pop-up"] floating enable
for_window [class="Gcr-prompter"] floating enable
for_window [class="Steam"] floating enable
# processing
for_window [class="processing-core-Applet"] floating enable
for_window [class="Xsane"] floating enable

for_window [class="Pinentry"] sticky enable
for_window [window_role="GtkFileChooserDialog"] resize set 800 600
for_window [window_role="GtkFileChooserDialog"] move position center
assign [class="Thunderbird"] $ws9
assign [class="Claws-mail"] $ws9
assign [class="Signal"] $ws9
assign [class="Emacs"] $ws6
assign [title="GIMP Startup"] $ws5
assign [class="Gimp"] $ws5
# set borders on new windows to 1pixel if they are in a split container. In
# stacked or tabbed containers the title bar(s) will still be shown if there is
# more than one window in the container.
new_window 1pixel
hide_edge_borders smart
popup_during_fullscreen leave_fullscreen
force_display_urgency_hint 500 ms
focus_on_window_activation none

# old eyes cannot see the subtle split indicator,so make it pink
# class                 border  backgr. text    indicator
client.focused          #4c7899 #005F87 #FFEFD9 #F42EE9
client.focused_inactive #333333 #5f676a #ffffff #7B0975
client.unfocused        #333333 #222222 #888888 #B31EAC
client.urgent           #2f343a #FF8700 #ffffff #FF8700

# Start some applets
exec dunst
exec nm-applet
exec --no-startup-id redshift-gtk -l 52:13
exec --no-startup-id blueman-applet
exec --no-startup-id seafile-applet
exec --no-startup-id xfce4-clipman

# Setttings
exec --no-startup-id xrdb -merge /home/niklas/.Xdefaults
exec --no-startup-id urxvtd -q -f -o
# disable touchpad while typing
exec --no-startup-id syndaemon -i 0.5 -K -d
exec --no-startup-id synclient PalmDetect=1
exec --no-startup-id synclient PalmMinWidth=8
exec --no-startup-id synclient PalmMinZ=100
exec --no-startup-id synclient VertScrollDelta=155

# no right click with two-finger tap on touchpad
exec --no-startup-id synclient TapButton2=0
# no composition because chrome makes sprite party
#exec compton -b --backend glx --paint-on-overlay
exec --no-startup-id xcompmgr
exec xmodmap /home/niklas/.Xmodmap
exec --no-startup-id wallpaper

# turn of coasting when scrolling with touchpad. Stop immediately
# else: 1) scroll fast 2) stop touching pad 3) press ctrl (in browser) => zoom
exec --no-startup-id xinput set-float-prop "SynPS/2 Synaptics TouchPad" "Synaptics Coasting Speed" 0 0

exec --no-startup-id ls ~/.bm* | entr ~/bin/tools/shortcuts

# Start autocutsel to sync clipboards
# 2014-01-03 annoying, cannot enter URLs on RTM, autoselecting fields overwrite selection
#exec --no-startup-id autocutsel -f -s PRIMARY
#exec --no-startup-id autocutsel -f -s CLIPBOARD
#exec --no-startup-id autocutsel -f -s SECONDARY


#exec --no-startup-id gvim /home/niklas/.i3/config
#exec --no-startup-id urxvt

# Start default apps
#exec pidgin
#exec google-chrome
#exec claws-mail

# chat
#workspace 8
